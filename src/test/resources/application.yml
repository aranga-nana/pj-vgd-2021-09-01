spring:
  profiles:
    active: test
    jpa:
      hibernate:
        ddl-auto: none
  sql:
    init:
      mode: never

application:
  throttle: 5
  weatherMapApiKey: add key for testing here
  ttl: 5
  apiBaseUrl: http://api.openweathermap.org/data/2.5/weather
  salt: securesalt

